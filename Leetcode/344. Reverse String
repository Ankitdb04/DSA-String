Using for loop:
class Solution {
    public void reverseString(char[] s) {
        int n=s.length;
        int i=0;
        for(int j=n-1;j>=0;j--){
            if(i<j){
                char temp=s[i];
                s[i]=s[j];
                s[j]=temp;
                i++;

            }else{
                break;
            }
        } return;
    }
}
Using for loop: Taking both pointer inside one loop only:
class Solution {
    public void reverseString(char[] s) {
        int n = s.length;

        for (int i = 0, j = n - 1; i < j; i++, j--) {

            char temp = s[i];
            s[i] = s[j];
            s[j] = temp;

        }
        return;
    }
Using while loop:
class Solution {
    public void reverseString(char[] s) {
        int n = s.length;
        int j=n-1;
        int i=0;
        while(i<j){
            char temp = s[i];
            s[i] = s[j];
            s[j] = temp;
            i++;
            j--;
        }
        return;
    }
}
}
